# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This project's actual guidance — commands, architecture, and (importantly) how to keep the hardcoded F1 data current — lives in [`AGENTS.md`](./AGENTS.md), the tool-agnostic canonical file. Read that file; nothing below duplicates it.

Treat `AGENTS.md` as authoritative for any other AI coding tool used on this repo too (Cursor, Copilot, Codex, Gemini, etc.) — if one of those needs its own entry file, it should be a pointer like this one, not a fork.
